#!/bin/bash
if [ $# -eq 0 ]
then 
    array=( web1.flipkart.com web2.flipkart.com web3.flipkart.com web4.flipkart.com )
else
    array="$@"
fi

for item in ${array[*]}
do
    resp=`curl --silent  -A"Mozilla/4.0" -I $item| head -n1` 
    echo $item" :: "$resp
done
